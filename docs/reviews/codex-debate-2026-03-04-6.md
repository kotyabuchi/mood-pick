# Codex Debate レビューレポート

> **日時**: 2026-03-04
> **対象**: Phase 6 Batch 1 — モックデータ削除
> **ラウンド数**: 1/5
> **採用指摘数**: 0 / 全2件

## 対象ファイル

| ファイル | 行数 | 種別 |
|----------|------|------|
| `src/lib/mock-data.ts` | 769 | 削除 |
| `src/__tests__/lib/mock-data.test.ts` | 176 | 削除 |
| `PROGRESS.md` | 6 | 更新 |

## Codex の指摘（2件）

| # | ファイル:行 | 問題 | 改善案 | 優先度 |
|---|------------|------|--------|--------|
| C-1 | mock-data.test.ts:1 | モックデータ import を禁止する lint ルールがない | `no-restricted-imports` ルール追加 | 低 |
| C-2 | mock-data.ts:1 | Storybook/E2E 等に残存参照の可能性 | `rg` で全体確認 | 低 |

## Claude の指摘（0件）

指摘なし。削除のみの変更でビルド・テスト全パス。

## 議論サマリー

### 合意点（初回で一致、即採用）

なし（指摘がいずれも低優先度で、対応不要と判断）

### 議論を経て却下

| 指摘 | 最終ラウンド | 却下理由 |
|------|:-----------:|---------|
| C-1 | R1 | ファイル自体が削除済みのため TypeScript コンパイラが import エラーを検出する。ESLint ルール追加は過剰 |
| C-2 | R1 | Grep で確認済み。本番コード・テスト・E2E に残存参照なし（PROGRESS.md と過去レビューレポートのみ） |

## 実施した改善

なし（指摘はいずれも却下）

## 検証結果

- lint/format: PASS (`pnpm check`)
- ビルド: PASS (`pnpm build`)
- テスト: PASS (`pnpm test` — 39 files, 296 tests)
- react-doctor: N/A（変更ソースファイルなし）

## 所見

Phase 6 Batch 1 は純粋な削除作業であり、リスクは最小限。全ページが Phase 1〜5 で実 DB/API に移行済みであることがビルド・テストで確認された。
